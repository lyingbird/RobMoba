# Roblox 手机端测试与 1v1 联机测试指引

## 结论先说
- 你们这个协作开发项目，做手机端 1v1 测试的正确路径是：
1. 在 Studio 里先用 `Device Emulator` 做界面和触控预检查。
2. 然后 `Publish to Roblox` 发布一个可访问的测试版本。
3. 在手机 Roblox App 上双人进入同一体验，做真机联机验证（这是 1v1 的关键）。
- 仅用 `Device Emulator` 不足以验证真实联网 1v1；它主要验证触控/分辨率/UI 显示。
- Roblox 游戏发布后不需要你自建服务器。Roblox 会按玩家进入情况自动分配和托管服务器实例。

## 你的问题：发布后服务器要自己搭吗？
- 不用自己组装服务器，也不用自己运维机器。
- 你负责的是游戏内容、脚本、配置、发布。
- 服务器实例由 Roblox 云端托管并按需启动；没有玩家时一般不会长期占用专属实例。
- 你如果做“固定同房间”的测试，一般通过邀请同一服务器、私有服务器或保留服务器（Reserved Server）机制完成，而不是自己买云服务器。

## 一、先在 Studio 里做手机模拟检查（单机预检查）

### 1. 打开 Device Emulator
1. 用 Roblox Studio 打开项目。
2. 顶部工具栏切到 `Test` 标签页。
3. 在 `Test` 菜单中找到 `Device Emulator` 并开启。

### 2. 选择手机机型与方向
1. 在 3D 视口上方找到 `device selector` 下拉框。
2. 选择一个手机设备配置（建议至少测一台低端机型和一台主流机型）。
3. 切换 `orientation`（竖屏 / 横屏）检查布局是否溢出、遮挡。
4. 根据需要调整视图尺寸（如果界面提供尺寸控制）。

### 3. 触控手势模拟
- 选中移动设备后，Touch simulation 会自动启用。
- 单指操作：鼠标点击拖动。
- 双指缩放/旋转：按住 `Alt` 再拖动。
- 双指平移：按住 `Alt + Shift` 再拖动。

### 4. 这里重点看什么
- 虚拟摇杆、按钮是否被刘海/圆角/安全区遮挡。
- 文本大小是否可读，按钮是否可点。
- 低分辨率下 UI 是否重叠。
- 触控输入与技能释放是否有误触。

> 注意：这一步不能替代“真机双人联机测试”。

## 二、发布一个可用于真机测试的版本

### 1. 从 Studio 发布
1. 顶部菜单点击 `File` -> `Publish to Roblox`。
2. 在 `Publish Game` 窗口填写名称、描述、设备支持（确认包含 Mobile）。
3. 完成发布。

### 2. 在 Creator Dashboard 配访问权限
1. 打开 Creator Dashboard（`https://create.roblox.com/dashboard/creations`）。
2. 进入你的体验。
3. 左侧进入 `Audience` -> `Access Settings`。
4. 根据测试阶段设置可见性（常用：仅测试成员可访问，或受控公开）。
5. 点击 `Save Changes`。

### 3. 协作项目建议
- 你们是多人开发，建议体验挂在 `Group` 名下，便于统一权限和后续扩展测试成员。
- 确保两位测试账号都具备访问该体验的权限。

## 三、手机端做 1v1 真机联机测试（重点）

### 方案 A（最快上手）：直接双人进场测试
1. 两台手机都安装 Roblox App，并登录两个不同账号。
2. 两个账号都能访问你发布的体验。
3. 由 A 账号发邀请给 B，或双方通过同一入口加入。
4. 进入后确认是否同服（看彼此是否可见、是否互相同步）。
5. 按测试清单跑完整个 1v1 回合。

适用：快速验证匹配、战斗同步、技能表现。

### 方案 B（更稳定）：用私有/保留服务器进行封闭 1v1
1. 配置私有服务器或在代码中用 `TeleportService`（如 `ReserveServer` / `TeleportToPrivateServer` / `TeleportAsync`）把两人送进同一保留服。
2. 两台手机按固定流程进入该测试房间。
3. 反复回归测试，避免陌生玩家干扰。

适用：你们要持续回归 1v1、需要稳定复现问题。

## 四、推荐的 1v1 手机测试清单（可直接照着跑）

### 基础连通
1. 双方是否稳定进入同一局。
2. 断线重连后状态是否正确。
3. 一方中途退出，另一方结算是否正确。

### 战斗同步
1. 位移、朝向、动画在对方视角是否一致。
2. 技能命中判定是否一致（本地命中与服务器结果是否冲突）。
3. 血量、倒地、胜负结算是否出现不同步。

### 手机体验
1. 30/60 FPS 档位下操作手感是否可接受。
2. 发热、掉帧、卡顿是否影响对战。
3. 高延迟（4G/弱网）下是否出现严重回滚或瞬移。

### 安全与反作弊（最低限）
1. 关键判定是否在服务端执行。
2. 客户端上报参数是否有边界校验。

## 五、你们当前协作开发下的建议流程（最实用）
1. 开发者 A 在 Studio 用 `Device Emulator` 先过一轮 UI/触控。
2. 开发者 A 发布测试版本。
3. 开发者 B 在手机真机加入，进行 1v1 联机回归。
4. 发现问题后回到 Studio 修复，再发布下一轮。
5. 每次发布记录版本号和改动点，避免“测不清测的是哪个版本”。

## 六、官方依据（你可以自行复核）
- Testing Modes（含 Team Test、Device Emulator、Touch simulation）
  - https://create.roblox.com/docs/studio/testing-modes
- Collaboration（多人协作 / Team Create）
  - https://create.roblox.com/docs/projects/collaboration
- Publish Experiences and Places（发布流程）
  - https://create.roblox.com/docs/production/publishing/publish-experiences-and-places
- Configure Experiences（访问设置）
  - https://create.roblox.com/docs/projects/configure-experiences
- TeleportService（保留服/传送相关 API）
  - https://create.roblox.com/docs/reference/engine/classes/TeleportService
- Teleporting（传送实务）
  - https://create.roblox.com/docs/projects/teleporting

## 七、你可以直接照抄的最短执行版
1. Studio: `Test` -> `Device Emulator`，先检查 UI 和触控。
2. Studio: `File` -> `Publish to Roblox`，发布测试版。
3. Dashboard: `Audience` -> `Access Settings`，放开给两位测试账号。
4. 两台手机进 Roblox App，同体验同服开打，跑 1v1 清单。
5. 如需稳定复现，改为私有/保留服务器再测。
