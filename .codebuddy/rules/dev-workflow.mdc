---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-03-10T04:56:21.948Z
provider: 
---

# 开发迭代工作流 — 自检 + 自修复 + 知识积累

## 核心原则
> 不要把"检查代码是否正确"的工作留给用户。每次修改必须自己验证，发现问题自己修复，然后告诉用户做了什么。

## 完整开发流程

### Phase 0: 恢复上下文（⚡ 快速启动，不要重新扫描项目）
1. **读 `.codebuddy/MEMORY.md`** — 这是项目的完整快照，包含：
   - 项目架构、Rojo 映射、所有 31 个文件清单
   - 模块 init() 签名、依赖关系图
   - 21 个 RemoteEvent 清单及参数
   - 英雄配色/技能配置
   - 文件修改时的同步检查清单
   → **读完这个文件后你就已经了解整个项目，不需要再 list_dir 或逐个读文件**
2. **读 `.codebuddy/memory/` 最近日志**（今天+昨天）了解最近改了什么
3. 只在需求涉及**具体代码逻辑**时才 read_file 打开对应模块
4. 如果需求涉及英雄技能机制，先查 `plan.md` 确认正确的技能定义

### Phase 1: 规划
1. 列出需要修改的文件清单
2. 查 MEMORY.md 的"文件修改时的快速 checklist"识别需要同步更新的文件
3. 检查 known-pitfalls 规则，标注可能触发的陷阱
4. 创建 TODO 跟踪进度

### Phase 2: 实现
1. 按依赖顺序修改（被依赖的模块先改）
2. 每改一个文件，立即检查：
   - `read_lints` 确认零错误
   - 新增的引用路径是否正确（参考 roblox-project 规则）
3. 如果修改了公开接口，立即用 `search_content` 找到所有调用方并同步更新

### Phase 3: 自检（对应 self-review 规则）
1. **Lint 全扫**: 所有修改文件 `read_lints`
2. **路径验证**: init 文件用 `script` 不是 `script.Parent`
3. **引用完整性**: 新模块在 init 中初始化了吗？
4. **接口一致性**: 改了函数签名，调用方同步了吗？
5. **按键冲突**: 新按键绑定和 QWER/ASD 冲突了吗？
6. **回归搜索**: grep 关键函数名确认无遗漏

### Phase 4: 自修复
- 如果自检发现问题，**不要告诉用户"我发现了个问题"然后等指令**
- **直接修复它**，然后在最终报告中说明：
  ```
  ⚠️ 自检修复: [描述问题] → [描述修复]
  ```

### Phase 5: 报告 + 知识积累
1. 给用户清晰的修改摘要（表格形式最佳）
2. 附加自检结果
3. 如果发现了新的陷阱/模式，更新 known-pitfalls 规则
4. **更新 MEMORY.md**: 如果新增/删除/重命名了文件、模块、Remote，同步更新 MEMORY.md
5. 将完成的工作追加到 `.codebuddy/memory/` 当日日志

## ⚠️ MEMORY.md 维护规则
MEMORY.md 是项目的**唯一真相源**，必须保持最新：
- **新增文件** → 更新文件清单表格
- **新增 RemoteEvent** → 更新 Remote 清单
- **修改模块 init 签名** → 更新初始化顺序和依赖关系
- **修改数据表结构** → 更新数据表引用关系
- **新增英雄** → 更新英雄配色和技能配置
- 不要等到最后再更新，**每次修改完就更新**

## 多文件修改的批处理策略
- 独立修改并行执行（如同时改 SkillTable 和 VFXTemplateTable）
- 有依赖的修改串行执行（先改 Remotes.luau，再改使用 Remote 的模块）
- 每批修改后做一轮自检，不要等全部改完才检查

## 错误升级策略
如果同一类错误出现 2 次：
1. 立即将其添加到 known-pitfalls 规则
2. 在 MEMORY.md 中更新相关约定
3. 考虑是否需要更新 roblox-project 规则中的禁止项

## 回复用户的格式模板
```markdown
## ✅ [任务描述] 完成

| 修改文件 | 改了什么 |
|---------|---------|
| xxx.luau | 描述 |

### 自检结果
✅ Lint: 0 错误 (X 文件)
✅ 引用: 已验证
✅ 接口: [无变更 / 已同步N处]
⚠️ 自修复: [如果有的话]
```