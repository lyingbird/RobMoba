# 2026-03-19

## 完成
- 确认仓库为唯一真相源
- 确认 Studio 只作为验证环境
- 建立 `./.codex-team/` 作为 Codex 专属工作流目录
- 本地安装 GSD for Codex 到 `./.codex/`
- 将仓库/Studio 主入口核对清单写入 Codex memory
- 修复 `HeroManager` 中未声明的 `regenThreads` 运行时 blocker

## 结论
- `.codebuddy/` 有参考价值，但不是当前运行依据
- 当前项目的最大难点是仓库与 Studio 存在两套系统分叉
- 下一步应优先做收敛，而不是直接扩功能
## Update: Training Validation Skill
- Added a tracked project skill source at `.codex-team/skills/roblox-training-validation/`
- Added a runtime Codex mirror at `.codex/skills/roblox-training-validation/`
- The skill captures the verified live training flow and acceptable fallback bridges for MCP validation

## Update: Boundary Refactor Start
- Began the repo-side boundary refactor instead of a full rewrite.
- Added `src/server/services/SkillLoadoutService.luau` to own player hero and replacement state.
- Added `src/server/services/BasicAttackService.luau` to own basic attack request resolution.
- Added `src/server/services/SkillTargeting.luau` to own direction and target position normalization.
- Rewired `src/server/SkillSystem.luau` to delegate to these services so the file starts moving toward orchestration-only responsibilities.
